# This static PWA currently requires no environment variables.
#
# If a future deployment adds analytics, backend sync, payments, or hosted API
# calls, document each variable here and keep real secrets out of the repository.
